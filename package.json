{
  "name": "ngo-web-design",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-sass": "node-sass src/main.scss dist/css/main.css",
    "watch-sass": "npm run compile-sass -- --watch",
    "start": "live-server"
  },
  "author": "Kilasa",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^9.0.0"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
